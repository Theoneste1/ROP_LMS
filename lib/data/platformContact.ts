import { env } from '../envConfig';

const platformContact = {
  email: ('info' + '@' + 'rwandaolympiadfoundation' + '.' + 'org') as string,
  phone: '+250 780 850 611' as string,
  address: 'Kigali, Rwanda' as string,
};

export default platformContact;

  